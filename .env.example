# FreeTimers currently runs in demo mode without environment variables.
#
# Provider-specific variables will be added here only when their adapters are
# selected and implemented. Each future entry must document whether it is
# public or server-only, whether it is required, and how to obtain and revoke it.
#
# Never place real secrets in this file. Copy it to .env.local before adding
# credentials for local development.
